package pa; // 이 파일의 모든 클래스는 pa 패키지 소속임을 선언

// public 클래스는 패키지 외부에서 사용 가능
public class TheClassOne {	// public 클래스 이름은 파일 이름과 같아야 합니다. -> 하나의 파일에는 하나의 public 클래스만 가능

}

// public이 아닌 클래스는 같은 패키지에서만 사용 가능
class PackageScopeClass { // public이 아닌 클래스는 이름을 자유롭게 사용 가능
	
}